# Project Rules & Workflows

## Git & Commit Workflow Rules
- **NEVER** run `git commit` or `git push` directly to `main` without explicit user permission.
- Always use a feature branch / Pull Request (PR) workflow (or get explicit approval first) before committing or pushing changes.
